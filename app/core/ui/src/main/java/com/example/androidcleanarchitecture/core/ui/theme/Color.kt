package com.example.androidcleanarchitecture.core.ui.theme

/**
 * Defines the color palette for the application's theme.
 * These colors are used to generate the [LightColorScheme] and [DarkColorScheme].
 */

import androidx.compose.ui.graphics.Color

// Light Theme Colors
/** Primary color for Light Theme (Lavender). */
val Purple80 = Color(0xFFD0BCFF)
/** Secondary color for Light Theme (Grey-ish Lavender). */
val PurpleGrey80 = Color(0xFFCCC2DC)
/** Tertiary color for Light Theme (Pink). */
val Pink80 = Color(0xFFEFB8C8)

// Dark Theme Colors
/** Primary color for Dark Theme (Deep Purple). */
val Purple40 = Color(0xFF6650a4)
/** Secondary color for Dark Theme (Deep Grey-ish Purple). */
val PurpleGrey40 = Color(0xFF625b71)
/** Tertiary color for Dark Theme (Deep Pink). */
val Pink40 = Color(0xFF7D5260)
